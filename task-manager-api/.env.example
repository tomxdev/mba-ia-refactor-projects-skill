# Copie para .env e ajuste. NUNCA versione segredos reais.
SECRET_KEY=troque-por-uma-chave-forte
DATABASE_URI=sqlite:///tasks.db
FLASK_DEBUG=false
HOST=0.0.0.0
PORT=5000
# Credenciais SMTP (opcional — notificações por e-mail).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
